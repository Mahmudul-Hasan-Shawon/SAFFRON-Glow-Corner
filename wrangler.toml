# Cloudflare Pages configuration — React SPA deployed from the dist/ build.
# Deploy with:
#   npx wrangler pages deploy dist --project-name glowsaffron --branch main
name = "glowsaffron"
compatibility_date = "2025-01-01"
pages_build_output_dir = "./dist"